<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button aria-controls="navbar" aria-expanded="false" data-target="#navbar" data-toggle="collapse" class="navbar-toggle collapsed" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="<%= root_path %>" class="navbar-brand">Crowd Pleaser</a>
    </div>
    <div class="collapse navbar-collapse" id="navbar">
      <ul class="nav navbar-nav">
        <% if current_user.present? %>
          <li class="active"><a href="<%= dashboards_path %>">Dashboard</a></li>
          <li class="active"><a href="<%= images_path %>">Images</a></li>
        <% end %>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if current_user.blank? %>
          <li><a href="<%= new_user_session_path %>">Sign in</a></li>
          <li><a href="<%= new_user_registration_path %>">Sign up</a></li>
        <% else %>
          <li><a href="javascript:void(0);"><%= current_user.username.capitalize %></a></li>
          <li><%= link_to('Sign out', destroy_user_session_path, :method => :delete) %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
<div class="clear"></div>
<div class="row-fluid">
  <div class="col-lg-3"></div>
  <div class="col-lg-6">
    <% if notice.present? %>
      <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert">
          <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
        </button>
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert">
          <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
        </button>
        <%= alert %>
      </div>
    <% end %>
  </div>
  <div class="col-lg-3"></div>
</div>
